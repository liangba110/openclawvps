
## 2026-08-28 16:10 mimo 接入 Codex 完成
- 老板提供小米 MiMo Token Plan 专属 token + base_url：https://token-plan-cn.xiaomimimo.com/v1（OpenAI 兼容，另有 /anthropic）
- 模型：mimo-v2.5-pro（推理模型，支持流式 SSE，content 输出前有 reasoning_content）
- **关键坑**：Codex wire_api 只认 responses，mimo 只兼容 chat/completions → 自写中转代理 /opt/mimo-proxy/proxy.mjs（node 零依赖，127.0.0.1:8787），把 responses 转 chat/completions
- 代理已用 systemd 托管：mimo-proxy.service（enabled + active，自启动自恢复）
- Codex 配置：~/.codex/config.toml 默认 model=mimo-v2.5-pro / provider=mimo，base_url=http://127.0.0.1:8787/v1
- token 存 ~/.codex/.mimo_token（chmod 600），config.toml 里 experimental_bearer_token 也写了一份
- 备份：config.toml.bak_20260828_1602
- deepseek provider 保留未删，可随时 -c model_provider="deepseek" 切回
- 事件序列必须：output_item.added → output_text.delta → output_text.done → output_item.done → response.completed（缺 done 事件 Codex 不显示输出）
- max_tokens 调 8000（太小推理会吃光 token 导致 content 空）
